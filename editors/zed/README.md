# Ziggy support for [Zed](https://zed.dev/)

The `Ziggy` extension can be installed from the extension store.

It provides grammar features, as syntax hihglighting and outlines.
If the ziggy executable is in your path, diagnostics from schema validation will be available.

Source is there: https://github.com/lvignoli/zed-ziggy.
